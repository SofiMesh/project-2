<%- include('../partials/header') %>

<table id="list">
    <thead>
        <tr>
            <th>Title</th>
            <!-- <th>Created<br>Year</th> -->
        </tr>
    </tread>
    <tbody>
        <% articles.forEach(function(a) { %>
            <tr>
                <td><%= a.title %></td>
                <td><%= a.createdYear %></td>
                <td><%= %></td>
                <td><a href="/articles/<%= a._id %>">DETAILS</a></td>
            </tr>
            <% }); %>
    </tbody>
</table>






<%- include('../partials/footer') %>
